# Optimized requirements for Brand Activation StreamDiffusion
# Target: 15-20+ FPS on NVIDIA L40S

# PyTorch with CUDA 11.8 - Required for L40S
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.1.0
torchvision==0.16.0

# XFormers - Latest compatible version for memory efficient attention
xformers==0.0.23

# Diffusers stack - Latest versions with all optimizations
diffusers==0.25.0
transformers==4.36.2
accelerate==0.26.1
safetensors==0.4.1
huggingface_hub==0.20.2

# StreamDiffusion (if available)
# git+https://github.com/cumulo-autumn/StreamDiffusion.git

# ONNX Runtime with TensorRT support (for TensorRT acceleration)
onnxruntime-gpu==1.16.3

# Web framework with performance optimizations
fastapi==0.109.0
uvicorn[standard]==0.27.0
uvloop==0.19.0  # High-performance event loop
websockets==12.0
python-multipart==0.0.6

# Image processing
opencv-python-headless==4.9.0.80
pillow==10.2.0
numpy==1.24.3

# Performance monitoring
psutil==5.9.8
GPUtil==1.4.0

# Optional: TensorRT (requires manual installation)
# tensorrt==8.6.1
