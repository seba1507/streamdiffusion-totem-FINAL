# requirements.txt (Golden Image Configuration)

# This set of versions is a known-good, stable combination for StreamDiffusion.
# We are pinning every single dependency to prevent pip from making bad decisions.

# PyTorch Stack (a stable 2.1.x version)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.1.2+cu121
torchvision==0.16.2+cu121
triton==2.1.0

# XFormers compatible with the torch version above
xformers==0.0.23

# Hugging Face Stack (a stable set that works together)
diffusers==0.24.0
transformers==4.36.2
accelerate==0.25.0
huggingface_hub==0.19.4
safetensors==0.4.1

# StreamDiffusion from Git
git+https://github.com/cumulo-autumn/StreamDiffusion.git@main#egg=streamdiffusion[tensorrt]

# Web Frameworks and other utils
fastapi==0.111.0
uvicorn[standard]==0.29.0
websockets==12.0
pillow==10.1.0
opencv-python-headless==4.8.1.78
scipy==1.11.4
polygraphy>=0.49.0
